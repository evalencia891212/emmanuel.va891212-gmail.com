import { DinamicFieldDirective } from './dynamic-field.directive';

describe('DinamicFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicFieldDirective();
    expect(directive).toBeTruthy();
  });
});
